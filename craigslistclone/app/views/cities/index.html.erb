<h1>Listing cities</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cities.each do |city| %>
  <tr>
    <td><%= link_to city.name, root_url(:subdomain => city.name) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New City', new_city_path %>